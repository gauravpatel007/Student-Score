# 🎓 Student ML Dashboard
A machine learning web application built with **Streamlit** that predicts student exam scores based on study habits.

## 🚀 Features
- **Score Prediction:** Uses a Linear Regression model (`model.pkl`) to predict grades.
- **Data Visualization:** Interactive charts using Matplotlib/Pandas.
- **User History:** Saves previous predictions in a SQLite database (`users.db`).

## 🛠️ Installation & Usage
1. Clone the repo: `git clone https://github.com/gauravpatel007/Student-Score.git`
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `streamlit run app.py`
